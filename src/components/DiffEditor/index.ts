export { default as DiffEditor } from './DiffEditor';
export type { 
  DiffEditorProps, 
  DiffEditorRef, 
  DiffStatistics, 
  SupportedLanguage, 
  EditorTheme, 
  DiffRenderMode 
} from './types';
export { 
  LANGUAGE_CONFIG_MAP, 
  THEME_CONFIG 
} from './types';
