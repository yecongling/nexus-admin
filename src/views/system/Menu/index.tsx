import type React from 'react';
import { useState } from 'react';

/**
 * 
 * @returns 菜单
 */
const Menu: React.FC = () => {
    // 当前选中的菜单项
    const [selectedMenu, setSelectedMenu] = useState();
  return <div>Menu</div>;
};

export default Menu;